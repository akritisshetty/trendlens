# TrendLens — Python Dependencies
# Install: pip install -r requirements.txt
# Python 3.11+ recommended (not 3.13 — PyTorch not yet supported)
# Tested on: Python 3.12, Linux x86_64

# ── Scientific computing / storage ──────────────────────────────────────
numpy>=1.26,<3.0
pandas>=2.2,<4.0
pyarrow>=14.0           # parquet I/O (needed by pandas.read_parquet)
scipy>=1.11
scikit-learn>=1.4
Pillow>=10.0

# ── Deep Learning ───────────────────────────────────────────────────────
torch>=2.2
torchvision>=0.17

# ── Vision-Language Models ──────────────────────────────────────────────
transformers>=4.40      # CLIP (image+text) and BLIP (captioning)
safetensors
huggingface_hub

# ── Vector search ───────────────────────────────────────────────────────
faiss-cpu>=1.8
sentence-transformers>=2.2.0

# ── Clustering & dim reduction ──────────────────────────────────────────
hdbscan>=0.8.33
umap-learn>=0.5.6
pynndescent>=0.5.13

# ── Visualisation ───────────────────────────────────────────────────────
matplotlib>=3.8
seaborn>=0.13

# ── Utilities ───────────────────────────────────────────────────────────
tqdm>=4.66
PyYAML>=6.0
requests>=2.31

# ── Testing ─────────────────────────────────────────────────────────────
pytest>=8.0
